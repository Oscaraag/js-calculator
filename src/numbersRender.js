cButton.addEventListener('click', () => {
  screenNumbers.textContent = ''
  requestOperation.textContent = ''
})

one.addEventListener('click', () => {
  let text = document.createTextNode(one.textContent)
  screenNumbers.appendChild(text)
})

two.addEventListener('click', () => {
  let text = document.createTextNode(two.textContent)
  screenNumbers.appendChild(text)
})
three.addEventListener('click', () => {
  let text = document.createTextNode(three.textContent)
  screenNumbers.appendChild(text)
})
four.addEventListener('click', () => {
  let text = document.createTextNode(four.textContent)
  screenNumbers.appendChild(text)
})
five.addEventListener('click', () => {
  let text = document.createTextNode(five.textContent)
  screenNumbers.appendChild(text)
})
six.addEventListener('click', () => {
  let text = document.createTextNode(six.textContent)
  screenNumbers.appendChild(text)
})
seven.addEventListener('click', () => {
  let text = document.createTextNode(seven.textContent)
  screenNumbers.appendChild(text)
})
eight.addEventListener('click', () => {
  let text = document.createTextNode(eight.textContent)
  screenNumbers.appendChild(text)
})
nine.addEventListener('click', () => {
  let text = document.createTextNode(nine.textContent)
  screenNumbers.appendChild(text)
})
zero.addEventListener('click', () => {
  let text = document.createTextNode(zero.textContent)
  screenNumbers.appendChild(text)
})
zero2.addEventListener('click', () => {
  let text = document.createTextNode(zero2.textContent)
  screenNumbers.appendChild(text)
})
DotButton.addEventListener('click', () => {
  let text = document.createTextNode(DotButton.textContent)
  screenNumbers.appendChild(text)
})
PlusOperator.addEventListener('click', () => {
  let text = document.createTextNode(PlusOperator.textContent)
  screenNumbers.appendChild(text)
})
MulOperator.addEventListener('click', () => {
  let text = document.createTextNode(MulOperator.textContent)
  screenNumbers.appendChild(text)
})

DivOperator.addEventListener('click', () => {
  let text = document.createTextNode(DivOperator.textContent)
  screenNumbers.appendChild(text)
})
restButton.addEventListener('click', () => {
  let text = document.createTextNode(restButton.textContent)
  screenNumbers.appendChild(text)
})
